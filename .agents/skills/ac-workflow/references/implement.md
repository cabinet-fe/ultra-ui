# implement

作为**资深开发工程师**执行当前计划中的全部步骤。

> **前提**：此协议的所有路径和编号均来自上下文脚本输出，禁止自行拼路径或扫描目录。

## 专业素养

阅读 `references/_principles.md` 的"实施与交付"段作为基线，不在此处重复。

## 前置检查

- `currentPlanStatus` 为 `null` 或 `"已执行"` → 终止执行。
- `## 目标` 或 `## 内容` 为空 → 通过「交互式提问工具」向用户获取缺失内容后更新 plan.md 并继续执行。
- 仅操作当前计划，不直接操作 `preparing/` 中的计划。
- 遇到阻塞问题应向用户报告，不可静默跳过。

## 执行步骤

1. **读取计划**：读取 `currentPlanFile` 指向的计划文件，准确理解 `## 目标` 与 `## 内容`。
2. **实施内容**：依据 `## 内容` 的步骤逐项实施。
3. **更新状态**：将 `plan.md` 的状态行修改为 `> 状态: 已执行`。
4. **记录范围**：更新 `## 影响范围`，详细记录本次变动的具体文件路径。`.agent-context/` 目录下的文件不计入影响范围。
5. **review 追问**：仅当满足**以下任一条件**时，通过「交互式提问工具」询问是否继续 review；否则静默结束。
   - 影响文件 ≥ 5 个，或跨 ≥ 2 个 package。
   - 涉及对外 API 改动、数据库迁移、安全敏感逻辑。
   - 实施过程中做出了计划未覆盖的显著决策（新增依赖、改变架构）。
