# agent-context — CLI

**权威 typings**（tsc 产物，仅含当前 tsconfig 声明输出）：[`generated/agent-context/`](../generated/agent-context/)

**说明**：以 CLI 为主；子命令与入口见 `packages/agent-context/src/cli.ts`（完整声明依赖 tsc 配置，未镜像的符号请直接读源码）。
