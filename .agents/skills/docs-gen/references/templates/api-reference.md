# 骨架：API 参考

适用于组件、类、Hook、高协同函数组，一个主题一篇，放 `agent-docs/components/` 或 `agent-docs/apis/`。`<>` 为占位符；章节名不得改动，章节要求见 [../doc-standards.md](../doc-standards.md)；写满的实例见 [../golden-sample.md](../golden-sample.md)。

````markdown
---
title: <导出名> <中文全称>
description: <是什么 + 解决什么场景 + 关键能力枚举，1~2 句。>
aliases: [<英文别名>, <中文别名>, <开源同类库叫法>]
keywords: [<公共属性名>, <方法名>, <错误类名>, <场景动宾短语>, <高频报错片段>]
---

# <导出名> <中文全称>

<一段概述：从哪个包导出什么，做什么，核心行为一句话。>

## 快速上手

```ts
import { <导出名> } from '<公共包名>';

<最少代码的自包含示例，含必要初始化>
// => <期望输出或行为>
```

## API 签名

```ts
<全部公共类型与签名，JSDoc 写默认值与约束>
```

## 参数说明

| 参数 | 类型 | 默认 | 必填 | 约束 |
| --- | --- | --- | :---: | --- |
| `<name>` | `<type>` | `<default>` | 是 / 否 | <取值范围、枚举、互斥或依赖> |

## 方法与事件

<实例方法、回调、Hook 返回值：签名、返回、同步 / 异步、抛出的错误与触发条件。无内容则删除本章节。>

## 典型示例

### <场景一名称>

```ts
import { <导出名> } from '<公共包名>';

<自包含示例>
```

### <场景二名称>

```ts
import { <导出名> } from '<公共包名>';

<自包含示例，import 重复写>
```

## 注意事项

> [!WARNING]
> - 本库是 `<X>`，不是 `<开源库里的 Y>`。
> - 禁止 <用法>，否则 <后果>。
> - `<旧 API>` 自 v<版本> 废弃，改用 `<新 API>`。

## 常见问题

### 报错 `<报错原文>`

原因：<一句话>。修复：

```ts
<修复代码>
```
````
