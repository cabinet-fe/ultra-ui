# 骨架：场景方案

适用于端到端业务场景，一个场景一篇，放 `agent-docs/recipes/`。`## 完整示例` 必须是可直接复制运行的端到端实现：全部文件、全部 import、初始化、调用、结果与错误处理。`<>` 为占位符；章节名不得改动，章节要求见 [../doc-standards.md](../doc-standards.md)。

````markdown
---
title: <库名> <场景名>
description: <解决什么业务场景，用到哪些导出。>
aliases: [<别名>]
keywords: [<用到的 API 名>, <场景词>, <同义场景词>]
---

# <库名> <场景名>

<一段概述：场景是什么、方案用到哪些导出。>

## 场景

- 何时用本方案：<条件>
- 何时不用：<条件>，改用 `<另一文档路径>`

## 完整示例

```ts
// <文件路径 1>
import { <导出名> } from '<公共包名>';

<端到端完整实现：初始化、调用、结果处理、错误处理>
```

```ts
// <文件路径 2>
<完整内容>
```

## 要点说明

- `<标识符或行为>`：<为什么这样写，不这样写会怎样>

## 注意事项

> [!WARNING]
> - <易错点>：<后果与正确写法>。
````
