# 骨架：排障

适用于运行时与构建期报错，放 `agent-docs/troubleshooting/`。每个问题一个 `## ` 章节，标题就是报错原文（含错误码）或症状短语——AI 拿报错原文搜到后可按原文直接取该节。每节必须给修复代码或命令，不只给文字。`<>` 为占位符；章节要求见 [../doc-standards.md](../doc-standards.md)。

````markdown
---
title: <库名> 常见报错
description: <库名> 运行时与构建期报错的原因与修复代码。
aliases: [FAQ, 排错, troubleshooting]
keywords: [<报错原文片段 1>, <报错原文片段 2>, <错误码>]
---

# <库名> 常见报错

每节一个问题，标题为报错原文；按报错原文直接取对应章节。

## <报错原文 1，含错误码>

原因：<一句话>。修复：

```ts
<修复代码>
```

## <报错原文 2>

原因：<一句话>。修复：

```bash
<修复命令>
```
````
