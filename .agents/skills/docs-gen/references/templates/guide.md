# 骨架：指南

适用于安装、配置、快速开始、概念说明，放 `agent-docs/guide/`。`## 完整示例` 是全部步骤合并后的最终形态，多文件时每个文件一个代码块、首行注释文件路径。`<>` 为占位符；章节名不得改动，章节要求见 [../doc-standards.md](../doc-standards.md)。

````markdown
---
title: <库名> <主题>
description: <完成后达到什么状态，覆盖哪些环境与版本。>
aliases: [<别名>]
keywords: [<命令>, <配置项名>, <环境变量名>, <场景词>]
---

# <库名> <主题>

<一段概述：本指南完成什么、适用版本与环境。>

## 前置条件

- <运行时>：`<精确版本范围>`
- <peer 依赖>：`<包名>@<版本>`
- <环境变量>：`<NAME>`（<取什么值，向谁索取>）

## 步骤

1. <动作>：

   ```bash
   <完整命令>
   ```

2. <动作>，写入 `<文件路径>`：

   ```ts
   // <文件路径>
   <完整文件内容>
   ```

## 完整示例

步骤合并后的最终形态，可直接复制：

```ts
// <文件路径 1>
<完整内容>
```

```ts
// <文件路径 2>
<完整内容>
```

## 验证

```bash
<验证命令>
# => <期望输出>
```

## 注意事项

> [!WARNING]
> - <常见误配置>：<后果与正确写法>。
````
