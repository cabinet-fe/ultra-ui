# @cat-kit/cli 示例

```bash
npx cat-cli verify-commit
```

（可执行名以安装包的 `package.json` 的 `bin` 字段为准。）

在代码中扩展 CLI 时，参见 [`generated/cli.d.ts`](generated/cli.d.ts) 与 [`generated/commands/`](generated/commands/)。
