---
name: cat-kit-cli
description: >
  Documents @cat-kit/cli (cat-cli) verify-commit and related commands. Use when wiring git hooks, commit verification, or CLI types from @cat-kit/cli.
---

# @cat-kit/cli

## 安装

```bash
npm add -D @cat-kit/cli
```

## 查证 API

[`generated/`](generated/) 下 **`.d.ts`** 与 npm `dist` 一致；CLI 发布入口为 `dist/cli.js`。

**运行环境**：Node.js。

## 更多

- [references/cli.md](references/cli.md)
- [examples.md](examples.md)
