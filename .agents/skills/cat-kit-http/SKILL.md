---
name: cat-kit-http
description: >
  Documents @cat-kit/http client with plugin architecture (retry, token, method override). Use when building HTTP clients, fetch wrappers, or integrating @cat-kit/http plugins.
---

# @cat-kit/http

## 安装

```bash
npm add @cat-kit/http
```

## 查证 API

[`generated/`](generated/) 下 **`.d.ts`** 与 npm `dist` 一致；入口 [`generated/index.d.ts`](generated/index.d.ts)。

**运行环境**：通用（基于标准 fetch 语义，与具体运行时绑定由调用方决定）。

## 更多

- [references/](references/)
- [examples.md](examples.md)
