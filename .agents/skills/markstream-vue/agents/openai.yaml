interface:
  display_name: Markstream Vue 3
  short_description: Wire markstream-vue modes in a Vue 3 app
  default_prompt: Use $markstream-vue to integrate markstream-vue into this Vue 3 repository with the right CSS order, renderer and code-block modes, optional peers, and streaming or virtual-scroll path.
