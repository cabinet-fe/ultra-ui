---
title: NAME_SPACE
description: Ultra UI 组件命名前缀常量，值为 U
---

`NAME_SPACE` 是 `@veltra/utils` 导出的组件命名前缀，值为 `'U'`。组件公开名按此前缀拼出，如 `UButton`、`USelect`。类名前缀见 `CLS_PREFIX`（`'u-'`）。

```ts
import { NAME_SPACE } from '@veltra/utils'

NAME_SPACE // 'U'
```
