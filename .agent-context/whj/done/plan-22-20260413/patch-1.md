# 同步 veltra-desktop 技能与 UBreadcrumb

## 补丁内容

计划 plan-22 已落地 `UBreadcrumb` 后，`skills/veltra-desktop` 的组件目录仍缺少 `breadcrumb` 条目，快速锚点也未指向新组件源码。本次在 `component-catalog.md` 的「导航与布局」中按字母序补充 `breadcrumb`，并在 `SKILL.md` 的快速源码锚点中增加 `breadcrumb.vue`，便于后续检索与跳转。

## 影响范围

- 修改文件: `skills/veltra-desktop/references/component-catalog.md`
- 修改文件: `skills/veltra-desktop/SKILL.md`
