{
  "name": "build",
  "type": "module",
  "scripts": {
    "build": "bun -e 'void 0'",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "release": "bun index.ts --release"
  },
  "devDependencies": {
    "@cat-kit/be": "^1.0.1",
    "@cat-kit/tsconfig": "^2.0.1",
    "@inquirer/prompts": "^8.4.1",
    "execa": "^9.6.1",
    "typescript": "^6.0.2"
  }
}
