# ULayout — 栅格布局

> `import type { LayoutProps } from '@veltra/desktop'`

> 类型：`../../../generated/types/layout.ts`

基于 CSS Grid 的布局容器组件，支持行列定义、间距、列宽拖拽调节。

## Import

```ts
// ULayout 由 Vite 自动导入，无需手动 import
```

> 示例见 [examples.md](./examples.md)
