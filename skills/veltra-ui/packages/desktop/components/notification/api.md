# UNotification - 通知

## 简介

用于命令式显示全局通知条。

## 类型文件

见 `./types.d.ts`

## 示例

见 `./examples.md`

## 辅助工具

本组件通常配合以下工具来使用。

### Notification

函数式通知条，命令式弹出。

使用示例:

```ts
import { Notification } from '@veltra/desktop'
```
