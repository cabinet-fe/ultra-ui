# UContextmenu - 右键菜单

## 类型文件

见 `./types.d.ts`

## 示例

见 `./examples.md`

## 辅助工具

本组件通常配合以下工具来使用。

### contextmenu

在鼠标位置弹出右键菜单（函数式 API）。

使用示例:

```ts
import { contextmenu } from '@veltra/desktop'
```
