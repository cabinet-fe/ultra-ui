# MessageConfirm - 消息确认

## 简介

用于命令式弹出需要用户确认的消息框。

## 类型文件

见 `./types.d.ts`

## 示例

见 `./examples.md`

## 辅助工具

本组件通常配合以下工具来使用。

### MessageConfirm

函数式确认框；另有 `primary` / `success` / `warning` / `danger` 等快捷方法。

使用示例:

```ts
import { MessageConfirm } from '@veltra/desktop'
```
