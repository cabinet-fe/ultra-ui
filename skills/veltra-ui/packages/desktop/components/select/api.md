# USelect — 单选选择器

> `import type { SelectProps, SelectEmits, SelectExposed } from '@veltra/desktop'`

> 类型：`../../../generated/types/select.ts`

基于下拉框的单选选择器，支持搜索过滤、远程异步加载、创建新选项、网格布局和虚拟滚动。

## Import

```ts
// USelect 由 Vite 自动导入，无需手动 import
```

> 示例见 [examples.md](./examples.md)
