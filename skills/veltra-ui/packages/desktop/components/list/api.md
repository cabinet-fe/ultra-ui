# UList — 列表

> `import type { ListProps } from '@veltra/desktop'`

> 类型：`../../../generated/types/list.ts`

基于数据驱动的通用列表组件，内部使用 `UScroll` 包裹，通过 scoped slot 自定义每个列表项的渲染。

## Import

```ts
// UList 由 Vite 自动导入，无需手动 import
```

> 示例见 [examples.md](./examples.md)
