# UBadge — 徽标

> `import type { BadgeProps, BadgeEmits, BadgeExposed } from '@veltra/desktop'`

> 类型：`../../../generated/types/badge.ts`

在子元素右上角显示数字、文本或圆点。

## Import

```ts
// UBadge 由 Vite 自动导入，无需手动 import
```

> 示例见 [examples.md](./examples.md)
