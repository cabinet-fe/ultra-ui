# UTheme — 主题编辑器

> `import type { ThemeProps } from '@veltra/desktop'`

## Import

```ts
import { UTheme } from '@veltra/desktop'
```

主题编辑组件，用于可视化配置和预览主题变量。Props 极简，内部集成主题面板。

## Examples

```vue
<u-theme />
```
