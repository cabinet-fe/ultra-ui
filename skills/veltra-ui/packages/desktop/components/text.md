# UText — 文本

> `import type { TextProps } from '@veltra/desktop'`

## Import

```ts
import { UText } from '@veltra/desktop'
```

文本展示组件，支持基础文字样式控制。Props 极简，用于渲染格式化文本内容。

## Examples

```vue
<u-text>普通文本内容</u-text>
```
