# UTextarea — 文本域

> `import type { TextareaProps, TextareaEmits } from '@veltra/desktop'`

## Import

```ts
import { UTextarea } from '@veltra/desktop'
```

继承 `InputProps`，API 与 `UInput` 一致，提供多行文本输入能力。

## Examples

```vue
<u-textarea v-model="text" placeholder="请输入" />
```
