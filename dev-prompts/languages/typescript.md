# TypeScript 代码风格指南

## 文件命名

遵循**kebab-case**命名法，例如：'user-manage.ts'

## 项目约定

### 类型定义偏好

- 严格类型定义，不要使用`any`类型
- 对象结构优先使用 `interface`（可扩展）
- 联合类型、工具类型使用 `type`
- 不得存在类型错误

### 导出偏好

- 使用具名导出，避免默认导出（支持 tree-shaking）

### 函数参数

- 超过 3 个参数时使用对象参数

### 注释要求

- 公共 API 必须有 JSDoc 注释
