# 待解决歧义（全局扫荡残留）

本文档记录在本次「类型 / 构建 / lint / 测试」扫荡中**未擅自大改**、需产品或团队决策后再动的项。未改动的 `tsconfig.json`、`vite.config.ts`、`tsdown.config.ts` 等工具配置均遵守任务约束。

## 1. oxlint：大量 `no-shadow`（约 80+ 处）

**现象**：内层 `watch` 回调、解构、`reduce` 等与外层 `props` / `ref` / 组合式变量同名，触发 `eslint(no-shadow)`。

**未批量改名原因**：改名涉及面广（desktop 组件、playground、compositions 等），易引入行为回归；部分阴影变量（如 `make-bem` 内 `e(name)` 与泛型参数 `name`）属于 API 形态问题，需统一风格后再动。

**可选方向**：专项 PR 按目录改名；或在 `.oxlintrc.json` 中对指定路径降级/关闭该规则（属配置变更，本次未做）。

## 2. oxlint：`eslint-plugin-unicorn` 若干条

**涉及**：`consistent-function-scoping`（内联函数是否外提）、`prefer-add-event-listener`（`FileReader.onerror` 等）、`no-empty-file` 已通过占位类型消除；若仍希望「零导出占位」与「Unicorn 空导出」策略一致，可再评审。

**未改原因**：事件监听改写可能影响错误处理语义；函数外提可能影响可读性或依赖闭包。

## 3. `@ultra-ui/desktop` 子路径与构建产物形态

**已做（package）**：在 `packages/desktop/package.json` 的 `exports` 中增加 `./components/*/style.ts`，使 `import …/style.ts` 在生产条件下映射到 `dist/components/*/style.js`，与 playground 中 `unplugin-vue-components` 生成的侧链导入一致。

**仍须知晓**：

- 子路径 `@ultra-ui/desktop/components`（无后续段）**没有**对应单一 `dist` 文件；聚合导出在包根 `export * from './components'`。演示代码中误用该路径会导致 Rolldown 解析失败，应使用 `@ultra-ui/desktop` 或具体子路径（如 `…/components/foo/style.ts`）。
- Node `createRequire` 与 ESM `import.meta.resolve` 对带 `.ts` 后缀子路径的行为不一致；以 bundler / `import.meta.resolve` 为准即可。

## 4. Playground 样式侧链

**约定**：对桌面组件样式的**显式**副作用导入，请使用 `…/style.ts`（与按需构建一致），避免直接 `…/style.scss`（`exports` 未保证 `.scss` 子路径可解析）。

## 5. 其他

- **`dynamic-form-model` 返回值**：已将 `errors.size > 0 ? false : true` 改为 `errors.size === 0`，语义等价；若业务期望「非布尔」或三态，需再确认。

---

_生成说明：随 plan-17 实施记录；后续清零某项时请删改对应条目并保留提交说明。_
