---
phase: 02-input-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/parser.ts
  - ui/components/expression-editor/__test__/parser.test.ts
autonomous: true

must_haves:
  truths:
    - "hello{foo}world round-trips correctly (trailing text preserved)"
    - "Plain text without variables parses and serializes correctly"
    - "Existing {variable} expressions remain parseable and serializable"
  artifacts:
    - path: ui/components/expression-editor/parser.ts
      provides: parseContent with trailing-text and no-match handling
      contains: "prevItem"
    - path: ui/components/expression-editor/__test__/parser.test.ts
      provides: Fixture tests for format round-trip
      contains: "parseContent"
  key_links:
    - from: ui/components/expression-editor/parser.ts
      to: VariableNode
      via: $createVariableNode returns node with getTextContent() = {variable}
      pattern: "\\{([^}]+)\\}"
    - from: ui/components/expression-editor/__test__/parser.test.ts
      to: parser.ts
      via: import parseContent, serialize nodes, assert round-trip
      pattern: "parseContent"
---

<objective>
Fix parser trailing-text bug and add format round-trip tests so {variable} expressions remain parseable, editable, and serializable (STAB-03).

Purpose: parseContent currently loses text after the last {variable}; add handling for no-match case. Add fixture tests to prevent regressions.
Output: Fixed parser.ts and parser.test.ts.
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-stability/02-RESEARCH.md
@.planning/codebase/TESTING.md
@ui/components/expression-editor/parser.ts
@ui/components/expression-editor/nodes/variable-node.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix parser trailing text and no-match case</name>
  <files>ui/components/expression-editor/parser.ts</files>
  <action>
Fix parseContent to preserve text after the last {variable} and handle plain text (Pitfall 4 in RESEARCH).

1. After the matchAll loop: if prevItem exists, compute `tail = content.slice(prevItem.index + prevItem[0].length)`. If tail is non-empty, push $createTextNode(tail) to nodes.

2. Handle no-match case: if prevItem is null (no variables found) and content is non-empty, push $createTextNode(content) to nodes.

Keep the existing regex /\{([^}]+)\}/g and loop logic unchanged. Only add the post-loop handling.
</action>
  <verify>bun run build passes; parseContent('hello{foo}world') produces nodes that serialize to 'hello{foo}world'</verify>
  <done>Text after last {...} is appended; plain text without variables produces a single TextNode</done>
</task>

<task type="auto">
  <name>Task 2: Add parser fixture tests</name>
  <files>ui/components/expression-editor/__test__/parser.test.ts</files>
  <action>
Add Vitest tests for parseContent round-trip. Serialize nodes by concatenating each node.getTextContent() (TextNode and VariableNode both implement getTextContent).

1. Create helper: `function serializeNodes(nodes: LexicalNode[]): string` returning nodes.map(n => n.getTextContent()).join('').

2. Test cases (parse → serialize → expect equals input):
   - `hello{foo}world` → round-trip 'hello{foo}world'
   - `{a}` → round-trip '{a}'
   - `hello` (no variables) → round-trip 'hello'
   - `{a}{b}` → round-trip '{a}{b}'
   - `prefix{a}suffix` → round-trip 'prefix{a}suffix'

3. Import parseContent from '../parser', LexicalNode from 'lexical'. Use describe/it/expect (Vitest globals).

Ensure ui/tsconfig exclude for __test__ does not block vitest (vitest runs tests separately). If vitest cannot resolve, add vitest workspace or adjust config. Per TESTING.md, vitest is in package.json; run with `bun vitest run`.
</action>
  <verify>bun vitest run ui/components/expression-editor/__test__/parser.test.ts passes</verify>
  <done>All fixture tests pass; format round-trip verified for edge cases</done>
</task>

</tasks>

<verification>
1. Run `bun run build` — must pass.
2. Run `bun vitest run ui/components/expression-editor/__test__/parser.test.ts` (or `bun vitest run` if path differs) — all tests pass.
</verification>

<success_criteria>
- STAB-03 satisfied: {variable} expressions parse, edit, and serialize correctly.
- Parser handles trailing text and no-match; fixture tests cover round-trip.
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-stability/02-03-SUMMARY.md`
</output>
