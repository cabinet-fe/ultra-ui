---
phase: 02-input-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/use-context.ts
autonomous: true

must_haves:
  truths:
    - "User can compose IME (Chinese/Japanese/Korean) without interruption when picker is open"
    - "PreventDefaultListener does not call preventDefault during active composition"
  artifacts:
    - path: ui/components/expression-editor/use-context.ts
      provides: IME-safe PreventDefaultListener
      contains: "isComposing"
  key_links:
    - from: ui/components/expression-editor/use-context.ts
      to: PreventDefaultListener
      via: early return when event.isComposing
      pattern: "isComposing"
---

<objective>
Ensure IME composition (Chinese/Japanese/Korean) is not interrupted when variable picker is open (STAB-02).

Purpose: PreventDefaultListener currently calls event.preventDefault() when contextVisible; this breaks IME during composition. Add isComposing check before any preventDefault.
Output: IME-safe use-context.ts.
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-stability/02-RESEARCH.md
@ui/components/expression-editor/use-context.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isComposing check to PreventDefaultListener</name>
  <files>ui/components/expression-editor/use-context.ts</files>
  <action>
Add IME-safety to PreventDefaultListener (Pitfall 3 in RESEARCH).

At the very start of PreventDefaultListener, before any other logic: add `if (event.isComposing) return false`. This lets the browser/IME handle composition events (commit, candidate selection) without preventDefault blocking them.

Keep the rest of the function unchanged: when not composing, if contextVisible.value, call event.preventDefault() and return true; otherwise return false.
</action>
  <verify>bun run build passes; PreventDefaultListener returns false when event.isComposing</verify>
  <done>PreventDefaultListener never calls preventDefault during active IME composition</done>
</task>

</tasks>

<verification>
1. Run `bun run build` — must pass.
2. Manual: In sample app with CJK IME, type `@` to open picker, then compose a Chinese/Japanese/Korean character. Composition should complete without corruption.
</verification>

<success_criteria>
- STAB-02 satisfied: IME composition works without interruption or corruption.
- use-context.ts PreventDefaultListener has `if (event.isComposing) return false` at top.
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-stability/02-02-SUMMARY.md`
</output>
