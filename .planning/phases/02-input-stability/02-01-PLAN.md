---
phase: 02-input-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/use-editor.ts
autonomous: true

must_haves:
  truths:
    - "User can type without cursor jump when v-model syncs from parent"
    - "User input is not overwritten when parent echoes back the same value"
    - "Selection is preserved during programmatic renderModelValue updates"
  artifacts:
    - path: ui/components/expression-editor/use-editor.ts
      provides: Model sync with equality check, lastEmittedValue guard, SKIP_DOM_SELECTION_TAG
      contains: "SKIP_DOM_SELECTION_TAG"
  key_links:
    - from: ui/components/expression-editor/use-editor.ts
      to: Lexical
      via: editor.update with tag SKIP_DOM_SELECTION_TAG
      pattern: "SKIP_DOM_SELECTION_TAG"
    - from: ui/components/expression-editor/use-editor.ts
      to: renderModelValue
      via: watchEffect skips when props.modelValue === current or changeByUser
      pattern: "lastEmittedValue|changeByUser"
---

<objective>
Eliminate cursor jump, text loss, and overwrite races when v-model syncs from parent (STAB-01).

Purpose: User types without interruption; parent updates that echo the same value do not trigger re-render; programmatic updates do not steal selection.
Output: Strengthened use-editor.ts with sync guard and SKIP_DOM_SELECTION_TAG.
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-stability/02-RESEARCH.md
@ui/components/expression-editor/use-editor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync guard with equality check and lastEmittedValue</name>
  <files>ui/components/expression-editor/use-editor.ts</files>
  <action>
Strengthen the model sync guard to prevent overwrite races (Pitfall 1 in RESEARCH).

1. Add `let lastEmittedValue: string | null = null`.

2. In registerTextContentListener: after emit, set `lastEmittedValue = text`. In the nextTick callback, compare `lastEmittedValue === props.modelValue`; if equal, set `changeByUser = false` immediately (parent echoed back). Otherwise use `setTimeout(() => { changeByUser = false }, 0)` to extend the guard window (avoids parent's watcher running before nextTick and overwriting).

3. In watchEffect (modelValue watcher): add early return when `props.modelValue === current` where `current = editor.getEditorState().read(() => $getRoot().getTextContent())`. Skip renderModelValue when values match—avoids redundant full rebuild when parent echoes.

4. Keep existing `if (changeByUser) return` at top of watchEffect.

Import $getRoot from lexical. Use nextTick and setTimeout as specified—do not use requestAnimationFrame.
</action>
  <verify>bun run build from repo root passes; no TypeScript errors in use-editor.ts</verify>
  <done>changeByUser is cleared only when lastEmittedValue matches modelValue or after deferred clear; watchEffect skips renderModelValue when modelValue equals current editor content</done>
</task>

<task type="auto">
  <name>Task 2: SKIP_DOM_SELECTION_TAG for renderModelValue</name>
  <files>ui/components/expression-editor/use-editor.ts</files>
  <action>
Use Lexical's SKIP_DOM_SELECTION_TAG when performing programmatic updates so cursor/selection is not lost (Pitfall 2 in RESEARCH).

1. Import SKIP_DOM_SELECTION_TAG from 'lexical'.

2. In renderModelValue: pass `{ tag: SKIP_DOM_SELECTION_TAG }` as second argument to editor.update(...). Example: `editor.update(() => { ... }, { tag: SKIP_DOM_SELECTION_TAG })`.

3. Move `nextTick(() => { changeByModel = false })` outside the editor.update callback—place it after the update call so changeByModel is cleared in nextTick after the update completes.
</action>
  <verify>bun run build passes; editor.update receives { tag: SKIP_DOM_SELECTION_TAG }</verify>
  <done>renderModelValue uses SKIP_DOM_SELECTION_TAG; DOM selection is not updated during programmatic sync</done>
</task>

</tasks>

<verification>
1. Run `bun run build` — must pass.
2. Manual: In sample app, bind expression editor to a reactive modelValue that updates (e.g. from parent watcher). Type text; parent echoes back; cursor should not jump and text should not be lost.
</verification>

<success_criteria>
- STAB-01 satisfied: No cursor jump, text loss, or overwrite when v-model syncs from parent.
- use-editor.ts contains SKIP_DOM_SELECTION_TAG, lastEmittedValue guard, and equality check in watchEffect.
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-stability/02-01-SUMMARY.md`
</output>
