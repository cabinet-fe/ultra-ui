---
phase: 06-repair-phase5-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/__test__/architecture-compatibility.test.ts
  - ui/components/expression-editor/__test__/architecture-boundaries.test.ts
  - ui/components/expression-editor/__test__/parser.test.ts
  - ui/components/expression-editor/__test__/drag-drop.test.ts
  - vitest.config.ts
  - package.json
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - STAB-03
  - UX-03
must_haves:
  truths:
    - "执行 `bun run test:phase5` 时不再出现 No test files found，且命令稳定成功。"
    - "ARCH-01/ARCH-02 的架构边界与公开 API 兼容性可通过自动化测试重复验证。"
    - "STAB-03/UX-03 的 parser 与 drag-drop 回归行为有稳定测试覆盖。"
    - "同一回归命令可在本地与 CI 工作流复用，无需额外人工步骤。"
  artifacts:
    - path: "ui/components/expression-editor/__test__/architecture-compatibility.test.ts"
      provides: "ARCH-02 公开导出、props、emits 兼容性闸门"
      contains: "UExpressionEditor|ExpressionEditorProps|update:modelValue"
    - path: "ui/components/expression-editor/__test__/architecture-boundaries.test.ts"
      provides: "ARCH-01 runtime/command packs/insertion/drag-drop 边界闸门"
      contains: "createExpressionEditorRuntime|registerCommandPacks|insertVariableAtTrigger|reorderVariable"
    - path: "ui/components/expression-editor/__test__/parser.test.ts"
      provides: "STAB-03 parse/edit/serialize 回归覆盖"
      contains: "parseContent"
    - path: "ui/components/expression-editor/__test__/drag-drop.test.ts"
      provides: "UX-03 变量拖拽重排与非法 payload 防退化覆盖"
      contains: "applyDropReorder|reorderVariableNode|moveVariableByDirection"
    - path: "package.json"
      provides: "Phase 5 单命令回归入口"
      contains: "test:phase5"
    - path: "vitest.config.ts"
      provides: "Vitest 对 .vue 与 @ui 别名解析能力"
      contains: "plugins: [vue()]|resolve.alias"
  key_links:
    - from: "package.json:test:phase5"
      to: "ui/components/expression-editor/__test__/*.test.ts"
      via: "显式目标文件列表"
      pattern: "architecture-compatibility|architecture-boundaries|parser|drag-drop"
    - from: "ui/components/expression-editor/__test__/architecture-compatibility.test.ts"
      to: "ui/types/components/expression-editor.ts"
      via: "类型契约断言"
      pattern: "ExpressionEditorProps|ExpressionEditorEmits"
    - from: "ui/components/expression-editor/__test__/architecture-boundaries.test.ts"
      to: "ui/components/expression-editor/internal"
      via: "能力入口连线断言"
      pattern: "createExpressionEditorRuntime|registerCommandPacks|insertVariableAtTrigger|reorderVariable"
    - from: "vitest.config.ts"
      to: "@ui/* imports in tests"
      via: "resolve.alias"
      pattern: "@ui"
---

<objective>
恢复 Phase 5 回归闸门的可重复自动化闭环：修复缺失测试目标、稳定测试入口 wiring，并将验证收敛到可重复执行的单命令。

Purpose: 把 Phase 5 的验证从“历史曾通过”恢复为“现在可持续、可复现、可复跑”。
Output: 回归测试文件恢复、vitest 解析链路稳定、`bun run test:phase5` 稳定通过。
</objective>

<execution_context>
@/home/whj/.codex/get-shit-done/workflows/execute-plan.md
@/home/whj/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repair-phase5-regression-gate/06-RESEARCH.md
@.planning/phases/05-architecture-refactor/05-03-SUMMARY.md
@package.json
@vitest.config.ts
@ui/types/components/expression-editor.ts
@ui/components/expression-editor/internal/editor-runtime/create-runtime.ts
@ui/components/expression-editor/internal/features/commands/register-command-packs.ts
@ui/components/expression-editor/internal/features/insertion/insertion-service.ts
@ui/components/expression-editor/internal/features/drag-drop/drag-drop-service.ts

<interfaces>
From ui/types/components/expression-editor.ts:
```typescript
export interface VariableItem {
  label: string;
  value: string;
  type?: string;
  children?: VariableItem[];
}

export interface ExpressionEditorProps extends FormComponentProps {
  modelValue?: string;
  placeholder?: string;
  variables?: VariableItem[];
}

export interface ExpressionEditorEmits {
  (e: 'update:modelValue', value: string): void;
}
```

From ui/components/expression-editor/internal/editor-runtime/create-runtime.ts:
```typescript
export function createExpressionEditorRuntime(
  options: CreateExpressionEditorRuntimeOptions
): ExpressionEditorRuntime;
```

From ui/components/expression-editor/internal/features/commands/register-command-packs.ts:
```typescript
export interface RegisterCommandPacksOptions {
  getContextCommands?: () => () => void;
}

export function registerCommandPacks(
  editor: LexicalEditor,
  options?: RegisterCommandPacksOptions
): () => void;
```

From ui/components/expression-editor/internal/features/insertion/insertion-service.ts:
```typescript
export function insertVariableAtTrigger(
  editor: LexicalEditor,
  input: InsertVariableInput
): boolean;
```

From ui/components/expression-editor/internal/features/drag-drop/drag-drop-service.ts:
```typescript
export function reorderVariable(
  editor: LexicalEditor,
  input: ReorderVariableInput
): boolean;

export function moveVariableByDirection(
  editor: LexicalEditor,
  sourceKey: string,
  direction: -1 | 1,
  focusMovedNode?: boolean
): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 恢复 Phase 5 回归测试资产并对齐需求映射</name>
  <files>
ui/components/expression-editor/__test__/architecture-compatibility.test.ts
ui/components/expression-editor/__test__/architecture-boundaries.test.ts
ui/components/expression-editor/__test__/parser.test.ts
ui/components/expression-editor/__test__/drag-drop.test.ts
  </files>
  <action>
按 `06-RESEARCH.md` 已确认来源，从 commit `8e4557e7` 恢复 4 个被误删测试文件到 `ui/components/expression-editor/__test__/`，优先使用 git 历史恢复而不是手写重写，避免断言语义漂移。
恢复后仅允许做“当前代码兼容性对齐”级别的小修（例如导入路径、describe 文案对齐需求 ID），禁止扩大测试范围或引入与 ARCH-01/ARCH-02/STAB-03/UX-03 无关的新断言。
确保 4 个测试分别稳定覆盖：公开 API 兼容、能力边界、parser round-trip、drag-drop 行为回归。
本任务仅完成“恢复完整性”校验，不承担执行性验证，避免对 Task 2 中 alias/config wiring 产生前置依赖。
  </action>
  <verify>
    <automated>test -f ui/components/expression-editor/__test__/architecture-compatibility.test.ts && test -f ui/components/expression-editor/__test__/architecture-boundaries.test.ts && test -f ui/components/expression-editor/__test__/parser.test.ts && test -f ui/components/expression-editor/__test__/drag-drop.test.ts</automated>
  </verify>
  <done>
4 个测试文件均恢复到位且内容完整，ARCH-01/ARCH-02/STAB-03/UX-03 的断言闭环基础恢复；执行性验证由 Task 2 完成。
  </done>
</task>

<task type="auto">
  <name>Task 2: 固化 test:phase5 入口与 Vitest 解析链路，验证可重复执行</name>
  <files>
package.json
vitest.config.ts
ui/components/expression-editor/__test__/architecture-compatibility.test.ts
ui/components/expression-editor/__test__/architecture-boundaries.test.ts
ui/components/expression-editor/__test__/parser.test.ts
ui/components/expression-editor/__test__/drag-drop.test.ts
  </files>
  <action>
校准 `package.json` 的 `test:phase5` 为稳定单命令入口，显式指向 4 个回归测试文件；若已有正确脚本，仅保留排序与可读性一致性调整，不改动无关脚本。
在 `vitest.config.ts` 保持 `@vitejs/plugin-vue` 与 `test.globals`，并补充 `resolve.alias`（`@ui` -> `ui`）以消除路径解析不确定性，确保 architecture 兼容测试在本地/CI 环境都可直接执行。
本任务承担首次执行性验证：在 alias/config wiring 就位后再进行回归命令跑通，避免前序任务重试抖动。
完成后连续执行两次 `bun run test:phase5`，若失败仅允许修复测试 wiring（路径、alias、配置），禁止修改业务实现代码以“迎合测试”。
  </action>
  <verify>
    <automated>bun run test:phase5 && bun run test:phase5</automated>
  </verify>
  <done>
`bun run test:phase5` 可重复通过，且命令具备 CI/本地复用稳定性，不再依赖人工临时排障。
  </done>
</task>

</tasks>

<verification>
1) `test -f ui/components/expression-editor/__test__/architecture-compatibility.test.ts && test -f ui/components/expression-editor/__test__/architecture-boundaries.test.ts && test -f ui/components/expression-editor/__test__/parser.test.ts && test -f ui/components/expression-editor/__test__/drag-drop.test.ts`
2) `bun run test:phase5`
3) `bun run test:phase5`（重复执行，验证可复现）
</verification>

<success_criteria>
- `bun run test:phase5` 能解析有效测试目标并稳定成功。
- ARCH-01/ARCH-02/STAB-03/UX-03 的回归检查全部纳入自动化并由同一入口触发。
- 回归闸门证据可通过同一命令在本地与 CI 工作流重复复跑。
</success_criteria>

<output>
After completion, create `.planning/phases/06-repair-phase5-regression-gate/06-01-SUMMARY.md`
</output>
