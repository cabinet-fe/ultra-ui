---
phase: 07-milestone-traceability-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md
  - .planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md
autonomous: true
requirements:
  - UX-01
  - UX-02
  - STAB-01
  - STAB-02
must_haves:
  truths:
    - "UX-01、UX-02、STAB-01、STAB-02 在跨源对账中均为 satisfied，不再出现 metadata drift 导致的 partial。"
    - "REQUIREMENTS 台账中四个目标 ID 的 checklist 与 traceability 状态与既有 VERIFICATION 事实一致。"
    - "Phase 7 SUMMARY frontmatter 的 requirements-completed 与 verification 覆盖完全一致且可机器读取。"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "四个目标需求的 checklist 与 Traceability 状态回写结果"
      contains: "UX-01|UX-02|STAB-01|STAB-02"
    - path: ".planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md"
      provides: "本 phase 的 requirements-completed 契约字段"
      contains: "requirements-completed: [UX-01, UX-02, STAB-01, STAB-02]"
    - path: ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"
      provides: "4-ID 三源对账矩阵与最终一致性结论"
      contains: "Final=satisfied"
  key_links:
    - from: ".planning/phases/02-input-stability/02-VERIFICATION.md + .planning/phases/03-variable-picker-interaction/03-VERIFICATION.md"
      to: ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"
      via: "Requirement-ID-led evidence reconciliation matrix"
      pattern: "STAB-01|STAB-02|UX-01|UX-02"
    - from: ".planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md"
      to: ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"
      via: "requirements-completed 列表与 verification 对账行一一对应"
      pattern: "requirements-completed"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"
      via: "checklist + traceability status 对齐 verification 事实"
      pattern: "\\[x\\].*(UX-01|UX-02|STAB-01|STAB-02)|Phase 7\\s*\\|\\s*Complete"
---

<objective>
完成里程碑元数据对账闭环：以需求 ID 为主键，同步 REQUIREMENTS、Phase 7 SUMMARY frontmatter 与 Phase 7 VERIFICATION 对账结果，消除跨源状态漂移。

Purpose: 关闭 INT-02 与 milestone-meta 追溯技术债，确保里程碑需求状态可重复审计。
Output: 可执行且可复核的 4-ID 对账结果（REQUIREMENTS 回写 + SUMMARY frontmatter 契约 + VERIFICATION 对账矩阵）。
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.5.0-MILESTONE-AUDIT.md
@.planning/phases/07-milestone-traceability-sync/07-RESEARCH.md
@.planning/phases/02-input-stability/02-VERIFICATION.md
@.planning/phases/03-variable-picker-interaction/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 以 4 个 requirement IDs 生成 Phase 7 对账事实矩阵</name>
  <files>.planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md</files>
  <action>
创建（或重建）`07-VERIFICATION.md`，仅围绕 `UX-01`、`UX-02`、`STAB-01`、`STAB-02` 建立三源矩阵，来源必须引用 Phase 2/3 既有 VERIFICATION 的满足证据，不重做功能测试。
矩阵列至少包含：REQ-ID、Verification（passed）、Summary requirements-completed（listed/missing）、REQUIREMENTS checklist、Traceability status、Final（satisfied/partial）。
在文件中明确记录 INT-02 的 drift 原因与本 phase 的修复口径，避免写成“新增功能验证”；严格限制改动在 `.planning/`，禁止触碰 `ui/components/**`。
  </action>
  <verify>
    <automated>rg -n "UX-01|UX-02|STAB-01|STAB-02|Final" ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"</automated>
  </verify>
  <done>`07-VERIFICATION.md` 含 4-ID 对账矩阵与 evidence 来源，且修复目标明确为 metadata reconciliation。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Phase 7 SUMMARY frontmatter 的 requirements-completed 契约</name>
  <files>.planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md</files>
  <action>
创建 `07-01-SUMMARY.md`（若已存在则更新）并按模板维护 frontmatter，`requirements-completed` 必须精确包含 `[UX-01, UX-02, STAB-01, STAB-02]`，顺序保持稳定。
`provides`/`key-decisions` 描述需明确“本 plan 仅做追溯元数据对账，不做运行时代码改动”；不要把 deferred scope 扩展到历史功能文件。
将 SUMMARY 内容与 Task 1 的 4-ID 矩阵保持一一对应，避免 `summary listed` 与 `verification passed` 不一致再次触发 partial。
  </action>
  <verify>
    <automated>node /home/whj/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get ".planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md" --field requirements-completed --raw</automated>
  </verify>
  <done>`07-01-SUMMARY.md` frontmatter 可解析，且 requirements-completed 完整覆盖四个目标需求 ID。</done>
</task>

<task type="auto">
  <name>Task 3: 回写 REQUIREMENTS 台账并完成跨源一致性收口</name>
  <files>.planning/REQUIREMENTS.md .planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md</files>
  <action>
执行 `requirements mark-complete` 将 `UX-01`、`UX-02`、`STAB-01`、`STAB-02` 的 checklist 与 Traceability 状态一次性对齐（避免手工逐行编辑遗漏）。
回写后更新 `07-VERIFICATION.md` 的最终矩阵，使四行结果全部为 `satisfied`，并明确“partial 已由 metadata drift 修复消除”。
完成后做双重核对：`REQUIREMENTS.md` 的四个 ID 状态为 complete；`07-VERIFICATION.md` 对应四行无 `partial`。如出现不一致，优先修正文档元数据而不是修改业务代码。
  </action>
  <verify>
    <automated>node /home/whj/.claude/get-shit-done/bin/gsd-tools.cjs requirements mark-complete UX-01 UX-02 STAB-01 STAB-02 && rg -n "UX-01|UX-02|STAB-01|STAB-02" ".planning/REQUIREMENTS.md" && rg -n "UX-01|UX-02|STAB-01|STAB-02" ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"</automated>
  </verify>
  <done>REQUIREMENTS、SUMMARY、VERIFICATION 三源对四个目标 ID 的状态一致，且不再存在因 metadata drift 导致的 partial 结论。</done>
</task>

</tasks>

<verification>
1) `node /home/whj/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter get ".planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md" --field requirements-completed --raw`
2) `rg -n "UX-01|UX-02|STAB-01|STAB-02" ".planning/REQUIREMENTS.md"`
3) `rg -n "UX-01|UX-02|STAB-01|STAB-02" ".planning/phases/07-milestone-traceability-sync/07-VERIFICATION.md"`
4) 核对 `07-VERIFICATION.md` 中 4-ID 最终状态均为 `satisfied`，且不再由 metadata drift 判定为 `partial`。
</verification>

<success_criteria>
- REQUIREMENTS checklist 与 traceability 状态与 Phase 2/3 verification 事实一致。
- Phase 7 SUMMARY frontmatter 的 `requirements-completed` 精确覆盖 `UX-01`、`UX-02`、`STAB-01`、`STAB-02`。
- 4-ID 跨源对账矩阵稳定为一致结果，不再出现 metadata drift 引发的 partial。
</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-traceability-sync/07-01-SUMMARY.md`
</output>
