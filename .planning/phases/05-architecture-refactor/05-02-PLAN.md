---
phase: 05-architecture-refactor
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - ui/components/expression-editor/plain-text.ts
  - ui/components/expression-editor/use-context.ts
  - ui/components/expression-editor/use-expression-drag-drop.ts
  - ui/components/expression-editor/internal/features/commands/register-command-packs.ts
  - ui/components/expression-editor/internal/features/insertion/insertion-service.ts
  - ui/components/expression-editor/internal/features/drag-drop/drag-drop-service.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "开发者可以单独调整 insertion 或 drag/drop 逻辑，而无需改动无关命令注册代码。"
    - "所有文档变更路径统一经过 typed mutation gateway，避免跨 feature 直接改共享引用。"
    - "命令优先级与键盘行为（含 IME 安全和 picker 双路径）保持与 Phase 2/3/4 一致。"
  artifacts:
    - path: "ui/components/expression-editor/internal/features/commands/register-command-packs.ts"
      provides: "按能力拆分并组合 command packs（text/clipboard/context/drag-drop）"
      contains: "mergeRegister"
    - path: "ui/components/expression-editor/internal/features/insertion/insertion-service.ts"
      provides: "变量插入 mutation gateway 实现"
      exports: ["insertVariableAtTrigger"]
    - path: "ui/components/expression-editor/internal/features/drag-drop/drag-drop-service.ts"
      provides: "拖拽重排 mutation gateway 实现"
      exports: ["reorderVariable"]
  key_links:
    - from: "ui/components/expression-editor/plain-text.ts"
      to: "ui/components/expression-editor/internal/features/commands/register-command-packs.ts"
      via: "plain-text 仅组装 command packs，不内联全部细节"
      pattern: "registerCommandPacks"
    - from: "ui/components/expression-editor/internal/features/commands/register-command-packs.ts"
      to: "ui/components/expression-editor/internal/features/insertion/insertion-service.ts"
      via: "insertion 命令通过 gateway 执行 mutation"
      pattern: "insertVariableAtTrigger"
    - from: "ui/components/expression-editor/internal/features/commands/register-command-packs.ts"
      to: "ui/components/expression-editor/internal/features/drag-drop/drag-drop-service.ts"
      via: "drag/drop 命令通过 gateway 执行重排"
      pattern: "reorderVariable"
---

<objective>
把当前 monolithic 命令处理拆成 capability command packs，并统一走 mutation gateway，建立可扩展的内部能力边界。

Purpose: 满足 ARCH-01 的“隔离修改”，同时为 ARCH-02 的内部扩展提供稳定插入点。
Output: command packs + insertion/drag-drop service + plain-text/use-context 的边界化接入。
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-architecture-refactor/05-RESEARCH.md
@.planning/phases/03-variable-picker-interaction/03-01-SUMMARY.md
@.planning/phases/04-drag-drop/04-01-SUMMARY.md
@ui/components/expression-editor/plain-text.ts
@ui/components/expression-editor/use-context.ts
@ui/components/expression-editor/use-expression-drag-drop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 plain-text 拆分为 capability command packs</name>
  <files>
ui/components/expression-editor/plain-text.ts
ui/components/expression-editor/internal/features/commands/register-command-packs.ts
  </files>
  <action>
把 `plain-text.ts` 当前聚合的命令逻辑拆分为明确 pack（text-editing、clipboard、context-keys、drag-drop），并在 `register-command-packs.ts` 使用 `mergeRegister` 统一装配。
必须保留 Lexical command priority 与返回语义（true/false）一致，避免行为漂移。
`plain-text.ts` 保留薄入口，负责注入依赖并调用 `registerCommandPacks`，不再承载所有 capability 细节。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
`rg "registerCommandPacks|mergeRegister|registerCommand" ui/components/expression-editor/plain-text.ts ui/components/expression-editor/internal/features/commands/register-command-packs.ts -n`
  </verify>
  <done>
命令注册已按能力拆包；修改任一 capability 时不需要编辑整份 `plain-text.ts` 的无关分支。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立 insertion/drag-drop typed mutation gateway</name>
  <files>
ui/components/expression-editor/internal/features/insertion/insertion-service.ts
ui/components/expression-editor/internal/features/drag-drop/drag-drop-service.ts
ui/components/expression-editor/use-expression-drag-drop.ts
  </files>
  <action>
将变量插入与拖拽重排的核心 mutation 统一封装为 typed service：所有写操作必须在 `editor.update` 内由 gateway 执行。
保留 Phase 4 的约束（variable-node-only、internal-only、silent revert、fallback 与 native 共用重排路径），并避免跨 feature 直接读写共享临时引用。
`use-expression-drag-drop.ts` 仅保留算法/slot 计算与状态辅助，mutation 入口迁移到 service 层。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
`bunx vitest run ui/components/expression-editor/__test__/drag-drop.test.ts`
  </verify>
  <done>
insertion 与 drag-drop 均通过 typed gateway 执行变更；drag/drop 行为回归测试保持通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: use-context/picker 链路改为契约化接入并保持既有交互</name>
  <files>
ui/components/expression-editor/use-context.ts
ui/components/expression-editor/plain-text.ts
ui/components/expression-editor/internal/features/commands/register-command-packs.ts
  </files>
  <action>
将 `use-context.ts` 与 command packs 的交互改为显式依赖注入/契约调用，避免 context 模块直接操作 insertion/drag-drop 内部细节。
保持 Phase 2/3 行为基线：IME 组合输入不被打断、picker 键盘双路径（editor command + picker input document listener）不退化。
若发现行为冲突，优先保留已有交互语义，再在 pack 内局部修正，不得回退到“集中式 if/else 大文件”。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
`bunx vitest run ui/components/expression-editor/__test__/parser.test.ts ui/components/expression-editor/__test__/drag-drop.test.ts`
  </verify>
  <done>
context 与命令处理以契约连接，IME + picker + drag/drop 关键路径回归通过，未新增跨模块耦合。
  </done>
</task>

</tasks>

<verification>
1) 类型校验：`bunx tsc -p ui/tsconfig.json --noEmit`
2) 回归测试：`bunx vitest run ui/components/expression-editor/__test__/parser.test.ts ui/components/expression-editor/__test__/drag-drop.test.ts`
3) 手动抽查：`@` 触发插入、键盘选择、变量重排、fallback 上下移均可用。
</verification>

<success_criteria>
- ARCH-01：sync/insertion/drag-drop/context 命令处理形成清晰边界，单点改动不再牵连大范围文件。
- ARCH-02：新 capability 可通过 command pack + gateway 接入，不要求改动公开 API。
- 命令优先级与关键交互（IME、picker、DnD）与既有行为一致。
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-refactor/05-02-SUMMARY.md`
</output>
