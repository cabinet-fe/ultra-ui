---
phase: 05-architecture-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/expression-editor.vue
  - ui/components/expression-editor/use-editor.ts
  - ui/components/expression-editor/internal/contracts/editor-runtime.ts
  - ui/components/expression-editor/internal/editor-runtime/create-runtime.ts
  - ui/components/expression-editor/internal/editor-runtime/model-sync.ts
  - ui/components/expression-editor/internal/editor-runtime/index.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "开发者可以在不改动外部 props/emits/导出的前提下调整 editor runtime 与 model sync。"
    - "sync 相关守卫（changeByUser/changeByModel/lastEmittedValue/SKIP_DOM_SELECTION_TAG）集中在独立模块。"
    - "expression-editor facade 仅负责公开接口与装配，不再承载核心业务实现。"
  artifacts:
    - path: "ui/components/expression-editor/internal/contracts/editor-runtime.ts"
      provides: "runtime 与 mutation 服务契约，定义 capability 边界"
      exports: ["ExpressionEditorRuntime", "EditorMutationGateway"]
    - path: "ui/components/expression-editor/internal/editor-runtime/model-sync.ts"
      provides: "modelValue <-> editorState 同步与守卫逻辑"
      contains: "lastEmittedValue|SKIP_DOM_SELECTION_TAG"
    - path: "ui/components/expression-editor/expression-editor.vue"
      provides: "稳定公共外观（props/emits）+ runtime 装配"
      contains: "update:modelValue"
  key_links:
    - from: "ui/components/expression-editor/expression-editor.vue"
      to: "ui/components/expression-editor/internal/editor-runtime/create-runtime.ts"
      via: "facade 调用 runtime 工厂完成初始化"
      pattern: "createExpressionEditorRuntime"
    - from: "ui/components/expression-editor/internal/editor-runtime/create-runtime.ts"
      to: "ui/components/expression-editor/internal/editor-runtime/model-sync.ts"
      via: "runtime 组合 sync 能力并暴露统一接口"
      pattern: "createModelSync"
---

<objective>
建立 Phase 5 的第一层架构边界：冻结公开 API，抽离 runtime/sync 契约，并让 facade 组件只承担装配职责。

Purpose: 先锁定“外部不变、内部可拆”的基线，避免后续能力拆分时引入 API 漂移。
Output: internal runtime contracts + runtime 工厂 + model sync 模块化落地，`expression-editor.vue` 外观瘦身。
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-architecture-refactor/05-RESEARCH.md
@.planning/phases/02-input-stability/02-01-SUMMARY.md
@ui/components/expression-editor/expression-editor.vue
@ui/components/expression-editor/use-editor.ts
@ui/types/components/expression-editor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 runtime 契约与工厂入口（API 冻结前提）</name>
  <files>
ui/components/expression-editor/internal/contracts/editor-runtime.ts
ui/components/expression-editor/internal/editor-runtime/create-runtime.ts
ui/components/expression-editor/internal/editor-runtime/index.ts
  </files>
  <action>
新增 internal contracts，显式定义 runtime 对外可用能力（例如 editor 实例访问、sync 触发、mutation gateway 入口），并在 runtime 工厂中集中装配依赖。
约束：contract 只描述 capability 边界，不泄露组件级 reactive refs；工厂只负责 wiring，不承载业务细节。
要求导出稳定命名（`createExpressionEditorRuntime`、`ExpressionEditorRuntime`），供 facade 与后续 command packs 复用。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
`rg "createExpressionEditorRuntime|interface ExpressionEditorRuntime|interface EditorMutationGateway" ui/components/expression-editor/internal -n`
  </verify>
  <done>
internal runtime contract 与工厂文件存在且可被类型检查通过；外部调用方可通过统一入口获取 runtime 能力而不依赖实现细节。
  </done>
</task>

<task type="auto">
  <name>Task 2: 抽离并封装 model sync 守卫到独立模块</name>
  <files>
ui/components/expression-editor/use-editor.ts
ui/components/expression-editor/internal/editor-runtime/model-sync.ts
ui/components/expression-editor/internal/editor-runtime/create-runtime.ts
  </files>
  <action>
将 `use-editor.ts` 中与模型同步相关的核心逻辑迁移到 `model-sync.ts`（含 equality check、lastEmittedValue、changeByUser/changeByModel、SKIP_DOM_SELECTION_TAG）。
保持 Phase 2 已验证行为不变：父级回写不覆盖输入、程序化更新不抢焦点、重复值不触发重渲染。
`use-editor.ts` 保留兼容层（委托到新模块），避免一次性删除导致调用面震荡。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
`bunx vitest run ui/components/expression-editor/__test__/parser.test.ts`
  </verify>
  <done>
sync 守卫实现聚合到 `model-sync.ts`，`use-editor.ts` 仅保留薄封装/转发，现有解析与基础编辑测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 3: facade 组件瘦身并只做公开接口装配</name>
  <files>
ui/components/expression-editor/expression-editor.vue
ui/components/expression-editor/internal/editor-runtime/create-runtime.ts
ui/components/expression-editor/internal/contracts/editor-runtime.ts
  </files>
  <action>
调整 `expression-editor.vue`：保留 `ExpressionEditorProps`、`update:modelValue`、现有对外行为入口，把 runtime/sync 初始化委托给 Task 1-2 的内部模块。
禁止在 facade 中新增业务规则（特别是 sync/insertion/drag-drop 细节）；如需能力扩展，通过 contract 注入。
确保对外导出路径与类型文件不变，为 ARCH-02 兼容性闸门预留稳定基线。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
`rg "defineProps<ExpressionEditorProps>|update:modelValue|createExpressionEditorRuntime" ui/components/expression-editor/expression-editor.vue -n`
  </verify>
  <done>
`expression-editor.vue` 成为外观层（public facade），内部能力通过 runtime 合约装配，未引入公开 API 变化。
  </done>
</task>

</tasks>

<verification>
1) 类型校验：`bunx tsc -p ui/tsconfig.json --noEmit`
2) 回归校验：`bunx vitest run ui/components/expression-editor/__test__/parser.test.ts`
3) 快速人工校验：在 sample 中输入 + 父级回写，确认无光标跳动/覆盖回归。
</verification>

<success_criteria>
- ARCH-01：sync 相关职责迁移到明确模块边界，修改 sync 不需要跨改 facade/drag-drop 代码。
- ARCH-02：`UExpressionEditor` 的 props/emits/导出路径保持不变，内部重构对外透明。
- 为下一计划提供稳定 runtime contract，可继续拆分 insertion/drag-drop/rendering。
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-refactor/05-01-SUMMARY.md`
</output>
