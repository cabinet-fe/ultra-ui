---
phase: 01-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/style.scss
  - ui/components/expression-editor/nodes/variable-node.tsx
  - ui/types/components/expression-editor.ts
  - ui/components/expression-editor/expression-editor.vue
autonomous: false

must_haves:
  truths:
    - "User can see expression editor using design tokens (color, border, radius, no default shadow)"
    - "User can see distinct visual feedback for default, focus, disabled, and readonly states"
    - "User can view variable nodes with chip/tag style, ellipsis + tooltip for long names, name + type indicator"
    - "User can view variable picker panel with consistent, non-flickering presentation"
  artifacts:
    - path: ui/components/expression-editor/style.scss
      provides: Design-token-based styling for editor, states, picker
      contains: "fn.use-var"
    - path: ui/components/expression-editor/nodes/variable-node.tsx
      provides: Variable node rendering via UTag
    - path: ui/types/components/expression-editor.ts
      provides: VariableItem type with optional type field
  key_links:
    - from: ui/components/expression-editor/style.scss
      to: design tokens
      via: fn.use-var() for all colors/spacing
      pattern: "fn.use-var"
    - from: ui/components/expression-editor/nodes/variable-node.tsx
      to: UTag component
      via: decorate() returns UTag
      pattern: "UTag"
    - from: ui/components/expression-editor/expression-editor.vue
      to: variable-node.tsx
      via: handleVariableSelect passes variable.type to $createVariableNode; updateVariableNode passes type from variableMap to node.updateVariable
      pattern: "handleVariableSelect|updateVariableNode"
---

<objective>
Align expression editor visuals with Ultra UI design language: design-token-based styling, distinct state feedback (default/hover/focus/disabled/readonly), and chip-style variable nodes plus elevated picker panel.

Purpose: Deliver UI-01, UI-02, UI-03 — visual consistency and non-flickering presentation for the expression editor.
Output: Updated style.scss, variable-node.tsx, and VariableItem type.
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation/01-CONTEXT.md
@.planning/phases/01-visual-foundation/01-RESEARCH.md
@ui/components/expression-editor/style.scss
@ui/components/expression-editor/nodes/variable-node.tsx
@ui/components/expression-editor/components/variable-picker.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editor container and state feedback</name>
  <files>ui/components/expression-editor/style.scss</files>
  <action>
Replace hardcoded colors and apply design tokens to the expression-editor block root and state modifiers. Per CONTEXT (locked):

1. **Default:** Use fn.use-var(bg-color, top), fn.use-var(border-width), fn.use-var(border-style), fn.use-var(border-color), fn.use-var(radius, default). Remove box-shadow entirely (CONTEXT: default shadow: none).

2. **Remove transition on state:** Remove `transition: all 0.2s ...` from the block root. State changes must be instant (CONTEXT: no visible transition animation).

3. **Hover:** Use fn.use-var(color, primary) for border-color. No box-shadow. Add :not(.is-disabled):not(.is-readonly) to hover selector.

4. **Focus:** Use border-color change only (fn.use-var(color, primary)), outline: none. Remove box-shadow from focus-within. Add :not(.is-disabled):not(.is-readonly).

5. **Disabled:** Use fn.use-var(color, disabled) for background, fn.use-var(border-color) for border. cursor: not-allowed. Remove opacity hacks. Ensure highly distinguishable at a glance.

6. **Readonly:** Use fn.use-var(bg-color, middle) for background, fn.use-var(border-color) for border. cursor: default. Use user-select: text for copy. No editable caret feel (CONTEXT: view/copy oriented).

7. **Spacing:** Compact density — use fn.use-var(gap, small) or smaller where appropriate for padding.

8. **Placeholder:** Replace #9ca3af with fn.use-var(text-color, placeholder).
9. **Container text:** Replace #1f2937 with fn.use-var(text-color, main).

Reference: ui/components/input/style.scss, ui/components/textarea/style.scss for token patterns.
Avoid: Any box-shadow on default; any transition on border/background/state properties.
  </action>
  <verify>grep -E 'fn\.use-var|#[0-9a-fA-F]{3,8}' ui/components/expression-editor/style.scss | head -50 — no raw hex for colors; fn.use-var present for block/placeholder/container. Run sample app and visually confirm default/focus/disabled/readonly states.</verify>
  <done>Editor block uses design tokens; no default shadow; no transition on state; focus uses border change only; disabled and readonly are clearly distinguishable.</done>
</task>

<task type="auto">
  <name>Task 2: Variable node styling and type indicator</name>
  <files>ui/components/expression-editor/nodes/variable-node.tsx</files>
  <files>ui/types/components/expression-editor.ts</files>
  <files>ui/components/expression-editor/style.scss</files>
  <files>ui/components/expression-editor/expression-editor.vue</files>
  <action>
Per CONTEXT (locked): chip/tag-like node, single accent, ellipsis + tooltip for long names, name + type indicator.

1. **VariableItem type:** Add optional `type?: string` to VariableItem in ui/types/components/expression-editor.ts (RESEARCH recommendation; backward compatible).

2. **VariableNode:** Accept optional type in constructor/serialization. In expression-editor.vue: update handleVariableSelect to pass variable.type to $createVariableNode when inserting a variable; update updateVariableNode to pass type from variableMap to node.updateVariable when syncing. Also update importJSON call sites if applicable. In decorate(): render label + short type indicator when type present (e.g. "name (type)" or compact badge). Use UTag with type='primary', round, size='small' per single-accent strategy. Add title={this.__label} (or equivalent) on the UTag or content for hover tooltip when label is long. Ensure UTag content supports ellipsis — UTag's u-tag__content already has overflow: hidden; text-overflow: ellipsis; white-space: nowrap.

3. **Remove unused var-block styles:** The .var-block and .var-node-icon BEM elements in style.scss are not used (variable-node returns UTag directly). Remove these rules to avoid confusion and align with "UTag as sole visual source" from RESEARCH.
  </action>
  <verify>VariableNode decorates with UTag; long label shows ellipsis; hover shows full label via title; type appears when provided. No .var-block or .var-node-icon in style.scss. bun run build succeeds.</verify>
  <done>Variable nodes use UTag with single accent; ellipsis + tooltip for long names; optional type indicator; no orphaned var-block styles.</done>
</task>

<task type="auto">
  <name>Task 3: Variable picker panel design tokens</name>
  <files>ui/components/expression-editor/style.scss</files>
  <action>
Replace hardcoded colors in variable picker panel elements with fn.use-var(). Per CONTEXT: panel visual hierarchy strong and elevated; candidate list density balanced.

1. **variable-list:** Replace #ffffff, #f3f4f6, #9ca3af, #6b7280 in background and scrollbar with fn.use-var(bg-color, top), fn.use-var(bg-color, middle), fn.use-var(text-color, placeholder), fn.use-var(text-color, second). Use fn.use-var for radius.

2. **variable-item, item:** Replace #1f2937, #f3f4f6, #ffffff with fn.use-var(text-color, main), fn.use-var(bg-color, hover), fn.use-var(color, primary). Use fn.use-var for box-shadow if needed.

3. **variable-empty:** Replace #6b7280 with fn.use-var(text-color, second).

4. **filter, breadcrumbs, breadcrumb-link, breadcrumb-separator:** Replace #f9fafb, #6b7280, #f3f4f6, #d1d5db, #9ca3af with fn.use-var(bg-color, middle), fn.use-var(text-color, second), fn.use-var(bg-color, hover), fn.use-var(border-color), fn.use-var(text-color, placeholder).

5. **panel, panel-list, panel-content, item:** Replace #f3f4f6, #1f2937, rgba(...) with fn.use-var. Ensure item active state uses fn.use-var(color, primary).

6. **Show/hide motion:** UTip wraps the picker. Tip already has transition (opacity + transform). Per CONTEXT: "subtle fade." If tip transition feels heavy, document; otherwise leave as-is (Claude's discretion per RESEARCH).

Avoid: Hardcoded hex colors; inconsistent density (keep balanced per CONTEXT).
  </action>
  <verify>grep -E '#[0-9a-fA-F]{3,8}' ui/components/expression-editor/style.scss — no remaining hex in picker-related blocks. Run sample app; panel shows with token-based styling; no flicker when opening/filtering.</verify>
  <done>Picker panel elements use design tokens; panel feels elevated; presentation is consistent and non-flickering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification</name>
  <files>N/A (human verification)</files>
  <action>Pause for human to verify the built expression editor visuals in the sample app.</action>
  <what-built>Design-token-based expression editor: container (no shadow, instant state transitions), variable nodes (chip/tag, ellipsis + tooltip, type), picker panel (elevated, token-based)</what-built>
  <how-to-verify>
1. Start sample app: `cd sample && bun run dev`
2. Navigate to expression-editor demo page
3. Default: Editor has border/background, no shadow
4. Focus: Click inside; border changes to primary color; no ring/shadow
5. States: Toggle disabled — grayed, cursor not-allowed; toggle readonly — distinct from disabled, cursor default, can select/copy
6. Variable nodes: Insert variable via @; node shows as chip; long name truncates with ellipsis; hover shows full name
7. Picker: Open with @; panel appears elevated; filter/search; no flicker; list readable
  </how-to-verify>
  <verify>Human runs sample app and confirms all visual states match CONTEXT decisions.</verify>
  <done>Human confirms visual verification complete; types "approved" or describes issues.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All hardcoded colors in expression-editor replaced with fn.use-var
- No box-shadow on default editor state
- No transition on state-related properties
- Disabled and readonly visually distinct
- Variable nodes use UTag exclusively; ellipsis + tooltip; type indicator when provided
- Picker panel uses design tokens; no flicker
</verification>

<success_criteria>
- UI-01: Expression editor uses design tokens
- UI-02: Default, focus, disabled, readonly distinguishable at a glance
- UI-03: Variable nodes and picker panel consistent, readable, non-flickering
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation/01-01-SUMMARY.md`
</output>
