---
phase: 04-drag-drop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/components/expression-editor/plain-text.ts
  - ui/components/expression-editor/use-expression-drag-drop.ts
  - ui/components/expression-editor/nodes/variable-node.tsx
  - ui/components/expression-editor/constants.ts
  - ui/components/expression-editor/expression-editor.vue
  - ui/components/expression-editor/style.scss
  - ui/components/expression-editor/__test__/drag-drop.test.ts
  - sample/src/expression-editor/index.vue
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "用户可以直接拖拽变量节点（{variable}）在当前表达式内重排。"
    - "拖拽后表达式文本内容严格保持，仅变量节点相对顺序改变。"
    - "拖拽过程中提供 ghost 和插入指示线，并在容器边缘自动滚动。"
    - "原生 DnD 不支持时，用户可用上移/下移完成等价重排。"
    - "文档明确边界规则并与实现保持一致。"
  artifacts:
    - path: "ui/components/expression-editor/plain-text.ts"
      provides: "Lexical DRAGSTART/DRAGOVER/DROP command pipeline with variable-only and internal-only guards"
      contains: "DRAGSTART_COMMAND, DROP_COMMAND"
    - path: "ui/components/expression-editor/use-expression-drag-drop.ts"
      provides: "Slot calculation, reorder engine, drop validation and auto-scroll shared by native DnD/fallback"
      exports: ["resolveDropSlot", "reorderVariableNode", "supportsNativeDnD"]
    - path: "ui/components/expression-editor/expression-editor.vue"
      provides: "Fallback move controls + insertion indicator wiring"
      contains: "move up / move down equivalent interaction"
    - path: "sample/src/expression-editor/index.vue"
      provides: "Behavior matrix for drag/drop rules and fallback semantics"
  key_links:
    - from: "ui/components/expression-editor/nodes/variable-node.tsx"
      to: "ui/components/expression-editor/plain-text.ts"
      via: "drag payload metadata + direct drag source marker"
      pattern: "setData\\('application/x-ultra-expression-variable'"
    - from: "ui/components/expression-editor/plain-text.ts"
      to: "ui/components/expression-editor/use-expression-drag-drop.ts"
      via: "drop slot resolution and lexical reorder"
      pattern: "resolveDropSlot|reorderVariableNode"
    - from: "ui/components/expression-editor/expression-editor.vue"
      to: "ui/components/expression-editor/use-expression-drag-drop.ts"
      via: "fallback move up/down uses same reorder function"
      pattern: "moveVariableByDirection|reorderVariableNode"
---

<objective>
实现 Phase 4 的变量节点拖拽重排（桌面优先）与原生 DnD 不可用时的等价交互，并保证行为可验证且与文档一致。

Purpose: 满足 UX-03，提供稳定、可预测、可维护的表达式重排交互。
Output: 完整的 drag/drop 命令链路、共享重排引擎、fallback 控件、测试与行为文档。
</objective>

<execution_context>
@/home/whj/.claude/get-shit-done/workflows/execute-plan.md
@/home/whj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-drag-drop/04-CONTEXT.md
@.planning/phases/04-drag-drop/04-RESEARCH.md
@ui/components/expression-editor/plain-text.ts
@ui/components/expression-editor/nodes/variable-node.tsx
@ui/components/expression-editor/expression-editor.vue
@sample/src/expression-editor/index.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 variable-node-only 拖拽命令链路与共享重排引擎</name>
  <files>
ui/components/expression-editor/plain-text.ts
ui/components/expression-editor/use-expression-drag-drop.ts
ui/components/expression-editor/nodes/variable-node.tsx
ui/components/expression-editor/constants.ts
  </files>
  <action>
在 `use-expression-drag-drop.ts` 新建拖拽域逻辑，封装合法 slot 计算、drop 合法性校验、节点重排和边缘自动滚动；该能力必须同时供原生 DnD 与 fallback 调用。
将 `plain-text.ts` 的 `DRAGSTART_COMMAND`/`DROP_COMMAND` TODO 替换为真实实现：只允许变量节点直接拖起、只接受当前表达式内部移动、拒绝外部数据源与跨区域目标、无效 drop 采用 silent revert（清状态不变更）。
在 `variable-node.tsx` 添加可识别的拖拽源标记（例如 data-* 与 draggable），并通过 DataTransfer 传递最小 payload（source key + move 语义）；重排必须通过 `editor.update` 完成，不得 DOM-only 操作。
选定离散策略：纯文本 hover 时 snap 到最近合法变量插槽；drop 后将焦点回到被移动变量，保证连续重排一致性。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
  </verify>
  <done>
`plain-text.ts` 中不再是 DnD no-op；仅变量节点可拖拽且仅内部重排生效；重排后表达式字符（含空白）严格保持不变。
  </done>
</task>

<task type="auto">
  <name>Task 2: 接入 ghost/插入指示与不支持原生 DnD 的等价控件</name>
  <files>
ui/components/expression-editor/expression-editor.vue
ui/components/expression-editor/style.scss
ui/components/expression-editor/use-expression-drag-drop.ts
  </files>
  <action>
在编辑器视图层接入拖拽状态：显示 ghost 态与插入指示线；dragover 期间启用容器边缘自动滚动。
增加 capability 检测（desktop-first）：当原生 DnD 不可用时显示变量节点的“上移/下移”控件；控件调用 Task 1 的同一重排函数，不允许实现第二套重排逻辑。
控件边界行为必须明确（首项禁上移、末项禁下移），并保证 readonly/disabled 下不触发重排。
保持 Phase 4 约束：不做移动端 touch-drag 扩展，仅提供文档化限制与 fallback。
  </action>
  <verify>
`bunx tsc -p ui/tsconfig.json --noEmit`
  </verify>
  <done>
原生 DnD 支持环境可见 ghost + insertion indicator + 自动滚动；不支持环境可通过上移/下移完成等价重排，且与拖放路径产出一致。
  </done>
</task>

<task type="auto">
  <name>Task 3: 补充拖拽约束测试并更新行为文档矩阵</name>
  <files>
ui/components/expression-editor/__test__/drag-drop.test.ts
sample/src/expression-editor/index.vue
  </files>
  <action>
新增 `drag-drop.test.ts`，覆盖至少这些场景：变量节点重排前后序列变化正确、纯文本及空白保持不变、外部/非法 payload 不产生变更、fallback 上移/下移与 drop 共用同一重排行为。
更新示例文档 `sample/src/expression-editor/index.vue`，增加拖拽行为矩阵：合法目标粒度、纯文本 hover 的 snap 规则、非法 drop 响应（silent revert）、drop 后焦点语义、桌面优先与 fallback 触发条件。
文档写明“实现为真源”：若后续实现变化，文档必须同步更新。
  </action>
  <verify>
`bunx vitest run ui/components/expression-editor/__test__/parser.test.ts ui/components/expression-editor/__test__/drag-drop.test.ts`
  </verify>
  <done>
测试可稳定验证 UX-03 关键约束；示例页包含可执行的交互规则说明，能直接作为实现/验收对照。
  </done>
</task>

</tasks>

<verification>
1) 静态校验：`bunx tsc -p ui/tsconfig.json --noEmit`
2) 测试校验：`bunx vitest run ui/components/expression-editor/__test__/parser.test.ts ui/components/expression-editor/__test__/drag-drop.test.ts`
3) 手动校验：`bun --filter sample run play` 后在表达式示例页验证
   - 变量节点可直接拖拽重排，文本与空白不变
   - 纯文本 hover 显示最近合法插入位
   - 无效 drop 不改内容，仅清理反馈状态
   - 原生 DnD 不可用时可用上移/下移完成同等重排
</verification>

<success_criteria>
- UX-03 达成：用户可通过原生拖拽或等价交互完成表达式重排，行为与文档一致。
- 所有锁定决策均被落实：变量节点限定、同区域内部、内容严格保持、ghost+插入指示、边缘自动滚动、fallback 上下移。
- 关键路径有自动化测试支撑，且示例文档可作为行为契约。
</success_criteria>

<output>
After completion, create `.planning/phases/04-drag-drop/04-01-SUMMARY.md`
</output>
