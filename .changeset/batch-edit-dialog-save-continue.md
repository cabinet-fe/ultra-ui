---
'@veltra/desktop': minor
---

UBatchEdit 弹框模式保存交互优化：

- 弹框内新增类操作（新增 / 插入 / 添加子级）底部新增「保存并继续」按钮：保存插入当前行后弹框保持打开，表单重置为初始默认值并推进插入点，可连续录入；原保存按钮行为不变（成功后关闭弹框）
- 弹框模式不再响应 `Ctrl/Cmd + S` 保存快捷键，底部快捷键提示同步移除（仅保留 Esc 关闭），保存只能走底部按钮；面板模式快捷键行为不变
