---
"@veltra/desktop": patch
"@veltra/utils": patch
"@veltra/styles": patch
"@veltra/compositions": patch
"@veltra/sheet": patch
---

适配 `@cat-kit/core` 1.2：用 `copy()` 替代已移除的 `o().copy()`，peer 最低版本提升至 1.2.1。
