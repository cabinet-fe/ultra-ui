---
'@veltra/desktop': minor
---

UForm 只读态样式重做，解决表单项「粘连」的观感：

- 行距修复：`gap` 原先经由 u-grid 内联样式写入（行距 0），CSS 只读覆盖从未生效；现改为表单级 CSS gap，只读态行距拉开为 `10px 12px`
- 只读态 label 去背景色并退为次级文本色，值区域保留浅色底并新增圆角与上下内边距，各项独立成块
- 非只读表单布局不变（gap `0 12px` 与原先一致）
