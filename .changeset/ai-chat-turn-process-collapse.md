---
'@veltra/ai': patch
---

修复思考过程与「生成出错 / 已停止生成」状态文字在默认主题下对比度过低的问题（`assist` → `second`）。

新增轮次级「已完成」折叠块：一轮对话中最终答案开始输出（或该轮结束）后，之前的思考与工具调用过程自动收起并卸载过程 DOM，点击可展开逐层钻取；regenerate 重跑时过程自动复位为实时可见。载入历史会话时思考块默认折叠。
