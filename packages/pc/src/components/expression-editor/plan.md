当前组件是基于 `lexical` 的表达式编辑器

接收以下变量格式:

```js
[
  {
    type: 'item',
    props: [
      {
        name: 'name',
        desc: '名称',
        type: 'string'
      }
    ]
  }
  {
    type: 'group'
    items: []
  }
]
```
