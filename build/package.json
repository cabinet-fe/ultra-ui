{
  "name": "build",
  "type": "module",
  "scripts": {
    "release": "bun ./index.ts",
    "build-only": "bun ./index.ts build-only"
  },
  "devDependencies": {
    "@builder/cli": "^3.0.0",
    "tsdown": "^0.15.12",
    "unplugin-vue": "^7.1.0"
  }
}
