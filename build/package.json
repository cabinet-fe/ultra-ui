{
  "name": "build",
  "type": "module",
  "scripts": {
    "build": "tsx ./index.ts",
    "pub": "cd ../dist && pnpm publish --registry https://192.168.31.250:6005",
    "release": "bumpp ../package.json && pnpm build && pnpm pub"
  }
}
