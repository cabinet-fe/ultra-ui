{
  "name": "build",
  "type": "module",
  "scripts": {
    "build": "tsx ./index.ts",
    "pub": "cd ../dist && pnpm publish --registry http://192.168.31.250:6005",
    "release": "pnpm build && bumpp ../package.json && pnpm pub"
  }
}
