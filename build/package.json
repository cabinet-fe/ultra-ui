{
  "name": "build",
  "type": "module",
  "scripts": {
    "build": "tsx ./index.ts",
    "release": "bumpp ../package.json && pnpm build && cd dist && pnpm publish --registry https://192.168.31.250:6005"
  }
}