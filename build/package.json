{
  "name": "build",
  "type": "module",
  "scripts": {
    "build": "bun ./index.ts",
    "pub": "cd ../dist && pnpm publish --registry http://192.168.31.250:6005",
    "gen:pkg": "tsx ./gen-package-json.ts",
    "release": "bun run build && bun bumpp ../package.json && bun run gen:pkg && bun pub"
  }
}
